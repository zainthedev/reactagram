import { PostCard } from '../styled-components/postStyles';
import { PostType } from '../types';

export const PostCardComponent = ({ caption, comments, image, location, poster }: PostType) => {
	return <PostCard>Home</PostCard>;
};
